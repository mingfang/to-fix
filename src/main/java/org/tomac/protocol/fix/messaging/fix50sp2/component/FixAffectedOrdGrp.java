package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixAffectedOrdGrp
{

	public int noAffectedOrders;
	public AffectedOrdGrp[] group;

	public void getAll(int noAffectedOrders, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noAffectedOrders = noAffectedOrders;

		if (noAffectedOrders < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noAffectedOrders ).getBytes(), FixTags.NOAFFECTEDORDERS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noAffectedOrders) {
			group = new AffectedOrdGrp[noAffectedOrders];

			for ( int i = 0; i < noAffectedOrders; i++ ) group[i] = new AffectedOrdGrp();
	}

		for ( int i = 0; i < noAffectedOrders; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noAffectedOrders; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noAffectedOrders; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noAffectedOrders; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixAffectedOrdGrp)) return false;

		FixAffectedOrdGrp msg = (FixAffectedOrdGrp) o;

		for (int i = 0; i<noAffectedOrders; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noAffectedOrders; i++)
			s += group[i].toString();
		return s;
	}

public class AffectedOrdGrp implements FixComponent
{

	public byte[] origClOrdID;
	public byte[] affectedOrderID;
	public byte[] affectedSecondaryOrderID;

	public AffectedOrdGrp() {
		super();

		origClOrdID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		affectedOrderID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		affectedSecondaryOrderID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( origClOrdID, (byte)0 );
		Utils.fill( affectedOrderID, (byte)0 );
		Utils.fill( affectedSecondaryOrderID, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.ORIGCLORDID_INT) {
				origClOrdID = FixUtils.getTagStringValue(value, origClOrdID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.AFFECTEDORDERID_INT) {
				affectedOrderID = FixUtils.getTagStringValue(value, affectedOrderID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.AFFECTEDSECONDARYORDERID_INT) {
				affectedSecondaryOrderID = FixUtils.getTagStringValue(value, affectedSecondaryOrderID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(origClOrdID)) return true;
		if (FixUtils.isSet(affectedOrderID)) return true;
		if (FixUtils.isSet(affectedSecondaryOrderID)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(origClOrdID)) FixUtils.putFixTag( out, FixTags.ORIGCLORDID_INT, origClOrdID, 0, Utils.lastIndexTrim(origClOrdID, (byte)0) );
		if (FixUtils.isSet(affectedOrderID)) FixUtils.putFixTag( out, FixTags.AFFECTEDORDERID_INT, affectedOrderID, 0, Utils.lastIndexTrim(affectedOrderID, (byte)0) );
		if (FixUtils.isSet(affectedSecondaryOrderID)) FixUtils.putFixTag( out, FixTags.AFFECTEDSECONDARYORDERID_INT, affectedSecondaryOrderID, 0, Utils.lastIndexTrim(affectedSecondaryOrderID, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(origClOrdID)) s += "OrigClOrdID(41)=" + new String(origClOrdID) + sep;
			if (FixUtils.isSet(affectedOrderID)) s += "AffectedOrderID(535)=" + new String(affectedOrderID) + sep;
			if (FixUtils.isSet(affectedSecondaryOrderID)) s += "AffectedSecondaryOrderID(536)=" + new String(affectedSecondaryOrderID) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof AffectedOrdGrp)) return false;

			AffectedOrdGrp msg = (AffectedOrdGrp) o;

		if (!Utils.equals( origClOrdID, msg.origClOrdID)) return false;

		if (!Utils.equals( affectedOrderID, msg.affectedOrderID)) return false;

		if (!Utils.equals( affectedSecondaryOrderID, msg.affectedSecondaryOrderID)) return false;

		return true;
	}
}
}
