package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.*;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixPtysSubGrp
{

	public int noPartySubIDs;
	public PtysSubGrp[] group;

	public void getAll(int noPartySubIDs, ByteBuffer buf) throws FixSessionException {
		this.noPartySubIDs = noPartySubIDs;

		if (noPartySubIDs < 1) throw new FixSessionException("asdasd");
		// this will leak memory if we grow the group
		if (group.length < noPartySubIDs) 
			group = new PtysSubGrp[noPartySubIDs];

		for ( int i = 0; i < noPartySubIDs; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noPartySubIDs; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noPartySubIDs; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noPartySubIDs; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noPartySubIDs; i++)
			s += group[i].toString();
		return s;
	}

public class PtysSubGrp implements FixComponent
{

	public byte[] partySubID;
	public long partySubIDType = 0;

	public PtysSubGrp() {
		super();

		partySubID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( partySubID, (byte)0 );
		partySubIDType = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.PARTYSUBID_INT) {
				partySubID = FixUtils.getTagStringValue(value, partySubID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.PARTYSUBIDTYPE_INT) {
				partySubIDType = FixUtils.getTagIntValue( value );
				if (!PartySubIDType.isValid(partySubIDType) ) throw new FixSessionException(buf, "Invalid enumerated value(" + partySubIDType + ") for tag: " + id );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
			if (id > 0) throw new FixSessionException(buf, "Required tag missing: " + id );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(partySubID)) return true;
		if (FixUtils.isSet(partySubIDType)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(partySubID)) FixUtils.putFixTag( out, FixTags.PARTYSUBID_INT, partySubID, 0, Utils.lastIndexTrim(partySubID, (byte)0) );
		if (FixUtils.isSet(partySubIDType)) FixUtils.putFixTag( out, FixTags.PARTYSUBIDTYPE_INT, partySubIDType);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(partySubID)) s += "PartySubID(523)=" + new String(partySubID) + sep;
			if (FixUtils.isSet(partySubIDType)) s += "PartySubIDType(803)=" + String.valueOf(partySubIDType) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof PtysSubGrp)) return false;

			PtysSubGrp msg = (PtysSubGrp) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( partySubID, msg.partySubID)) return false;

		if (!( partySubIDType==msg.partySubIDType)) return false;

		return true;
	}
}
}
