package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixDerivativeInstrumentAttribute
{

	public int noDerivativeInstrAttrib;
	public DerivativeInstrumentAttribute[] group;

	public void getAll(int noDerivativeInstrAttrib, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noDerivativeInstrAttrib = noDerivativeInstrAttrib;

		if (noDerivativeInstrAttrib < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noDerivativeInstrAttrib ).getBytes(), FixTags.NODERIVATIVEINSTRATTRIB_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noDerivativeInstrAttrib) {
			group = new DerivativeInstrumentAttribute[noDerivativeInstrAttrib];

			for ( int i = 0; i < noDerivativeInstrAttrib; i++ ) group[i] = new DerivativeInstrumentAttribute();
	}

		for ( int i = 0; i < noDerivativeInstrAttrib; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noDerivativeInstrAttrib; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noDerivativeInstrAttrib; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noDerivativeInstrAttrib; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixDerivativeInstrumentAttribute)) return false;

		FixDerivativeInstrumentAttribute msg = (FixDerivativeInstrumentAttribute) o;

		for (int i = 0; i<noDerivativeInstrAttrib; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noDerivativeInstrAttrib; i++)
			s += group[i].toString();
		return s;
	}

public class DerivativeInstrumentAttribute implements FixComponent
{

	public long derivativeInstrAttribType = 0;
	public byte[] derivativeInstrAttribValue;

	public DerivativeInstrumentAttribute() {
		super();

		derivativeInstrAttribValue = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		derivativeInstrAttribType = Long.MAX_VALUE;		
		Utils.fill( derivativeInstrAttribValue, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.DERIVATIVEINSTRATTRIBTYPE_INT) {
				derivativeInstrAttribType = FixUtils.getTagIntValue(null ,id ,value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.DERIVATIVEINSTRATTRIBVALUE_INT) {
				derivativeInstrAttribValue = FixUtils.getTagStringValue(null ,id ,value, derivativeInstrAttribValue);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(derivativeInstrAttribType)) return true;
		if (FixUtils.isSet(derivativeInstrAttribValue)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(derivativeInstrAttribType)) FixUtils.putFixTag( out, FixTags.DERIVATIVEINSTRATTRIBTYPE_INT, derivativeInstrAttribType);
		if (FixUtils.isSet(derivativeInstrAttribValue)) FixUtils.putFixTag( out, FixTags.DERIVATIVEINSTRATTRIBVALUE_INT, derivativeInstrAttribValue, 0, Utils.lastIndexTrim(derivativeInstrAttribValue, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(derivativeInstrAttribType)) s += "DerivativeInstrAttribType(1313)=" + String.valueOf(derivativeInstrAttribType) + sep;
			if (FixUtils.isSet(derivativeInstrAttribValue)) s += "DerivativeInstrAttribValue(1314)=" + new String(derivativeInstrAttribValue) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof DerivativeInstrumentAttribute)) return false;

			DerivativeInstrumentAttribute msg = (DerivativeInstrumentAttribute) o;

		if (!( derivativeInstrAttribType==msg.derivativeInstrAttribType)) return false;

		if (!Utils.equals( derivativeInstrAttribValue, msg.derivativeInstrAttribValue)) return false;

		return true;
	}
}
}
