package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixMatchRules
{

	public int noMatchRules;
	public MatchRules[] group;

	public void getAll(int noMatchRules, ByteBuffer buf) throws FixSessionException {
		this.noMatchRules = noMatchRules;

		if (noMatchRules < 1) throw new FixSessionException("asdasd");
		// this will leak memory if we grow the group
		if (group == null || group.length < noMatchRules) {
			group = new MatchRules[noMatchRules];

			for ( int i = 0; i < noMatchRules; i++ ) group[i] = new MatchRules();
	}

		for ( int i = 0; i < noMatchRules; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noMatchRules; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noMatchRules; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noMatchRules; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noMatchRules; i++)
			s += group[i].toString();
		return s;
	}

public class MatchRules implements FixComponent
{

	public byte[] matchAlgorithm;
	public byte[] matchType;

	public MatchRules() {
		super();

		matchAlgorithm = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		matchType = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( matchAlgorithm, (byte)0 );
		Utils.fill( matchType, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.MATCHALGORITHM_INT) {
				matchAlgorithm = FixUtils.getTagStringValue(value, matchAlgorithm);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.MATCHTYPE_INT) {
				matchType = FixUtils.getTagStringValue(value, matchType);
				if (!FixMessageInfo.MatchType.isValid(matchType) ) throw new FixSessionException(buf, "Invalid enumerated value(" + matchType + ") for tag: " + id );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
			if (id > 0) throw new FixSessionException(buf, "Required tag missing: " + id );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(matchAlgorithm)) return true;
		if (FixUtils.isSet(matchType)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(matchAlgorithm)) FixUtils.putFixTag( out, FixTags.MATCHALGORITHM_INT, matchAlgorithm, 0, Utils.lastIndexTrim(matchAlgorithm, (byte)0) );
		if (FixUtils.isSet(matchType)) FixUtils.putFixTag( out, FixTags.MATCHTYPE_INT, matchType, 0, Utils.lastIndexTrim(matchType, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(matchAlgorithm)) s += "MatchAlgorithm(1142)=" + new String(matchAlgorithm) + sep;
			if (FixUtils.isSet(matchType)) s += "MatchType(574)=" + new String(matchType) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof MatchRules)) return false;

			MatchRules msg = (MatchRules) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( matchAlgorithm, msg.matchAlgorithm)) return false;

		if (!Utils.equals( matchType, msg.matchType)) return false;

		return true;
	}
}
}
