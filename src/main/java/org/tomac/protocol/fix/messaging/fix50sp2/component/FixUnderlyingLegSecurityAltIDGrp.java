package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixUnderlyingLegSecurityAltIDGrp
{

	public int noUnderlyingLegSecurityAltID;
	public UnderlyingLegSecurityAltIDGrp[] group;

	public void getAll(int noUnderlyingLegSecurityAltID, ByteBuffer buf) throws FixSessionException {
		this.noUnderlyingLegSecurityAltID = noUnderlyingLegSecurityAltID;

		if (noUnderlyingLegSecurityAltID < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noUnderlyingLegSecurityAltID ).getBytes(), FixTags.NOUNDERLYINGLEGSECURITYALTID_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noUnderlyingLegSecurityAltID) {
			group = new UnderlyingLegSecurityAltIDGrp[noUnderlyingLegSecurityAltID];

			for ( int i = 0; i < noUnderlyingLegSecurityAltID; i++ ) group[i] = new UnderlyingLegSecurityAltIDGrp();
	}

		for ( int i = 0; i < noUnderlyingLegSecurityAltID; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noUnderlyingLegSecurityAltID; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noUnderlyingLegSecurityAltID; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noUnderlyingLegSecurityAltID; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noUnderlyingLegSecurityAltID; i++)
			s += group[i].toString();
		return s;
	}

public class UnderlyingLegSecurityAltIDGrp implements FixComponent
{

	public byte[] underlyingLegSecurityAltID;
	public byte[] underlyingLegSecurityAltIDSource;

	public UnderlyingLegSecurityAltIDGrp() {
		super();

		underlyingLegSecurityAltID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		underlyingLegSecurityAltIDSource = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( underlyingLegSecurityAltID, (byte)0 );
		Utils.fill( underlyingLegSecurityAltIDSource, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.UNDERLYINGLEGSECURITYALTID_INT) {
				underlyingLegSecurityAltID = FixUtils.getTagStringValue(value, underlyingLegSecurityAltID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.UNDERLYINGLEGSECURITYALTIDSOURCE_INT) {
				underlyingLegSecurityAltIDSource = FixUtils.getTagStringValue(value, underlyingLegSecurityAltIDSource);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(underlyingLegSecurityAltID)) return true;
		if (FixUtils.isSet(underlyingLegSecurityAltIDSource)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(underlyingLegSecurityAltID)) FixUtils.putFixTag( out, FixTags.UNDERLYINGLEGSECURITYALTID_INT, underlyingLegSecurityAltID, 0, Utils.lastIndexTrim(underlyingLegSecurityAltID, (byte)0) );
		if (FixUtils.isSet(underlyingLegSecurityAltIDSource)) FixUtils.putFixTag( out, FixTags.UNDERLYINGLEGSECURITYALTIDSOURCE_INT, underlyingLegSecurityAltIDSource, 0, Utils.lastIndexTrim(underlyingLegSecurityAltIDSource, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(underlyingLegSecurityAltID)) s += "UnderlyingLegSecurityAltID(1335)=" + new String(underlyingLegSecurityAltID) + sep;
			if (FixUtils.isSet(underlyingLegSecurityAltIDSource)) s += "UnderlyingLegSecurityAltIDSource(1336)=" + new String(underlyingLegSecurityAltIDSource) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof UnderlyingLegSecurityAltIDGrp)) return false;

			UnderlyingLegSecurityAltIDGrp msg = (UnderlyingLegSecurityAltIDGrp) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( underlyingLegSecurityAltID, msg.underlyingLegSecurityAltID)) return false;

		if (!Utils.equals( underlyingLegSecurityAltIDSource, msg.underlyingLegSecurityAltIDSource)) return false;

		return true;
	}
}
}
