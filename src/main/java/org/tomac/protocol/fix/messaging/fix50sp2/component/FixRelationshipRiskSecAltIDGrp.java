package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixRelationshipRiskSecAltIDGrp
{

	public int noRelationshipRiskSecurityAltID;
	public RelationshipRiskSecAltIDGrp[] group;

	public void getAll(int noRelationshipRiskSecurityAltID, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noRelationshipRiskSecurityAltID = noRelationshipRiskSecurityAltID;

		if (noRelationshipRiskSecurityAltID < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noRelationshipRiskSecurityAltID ).getBytes(), FixTags.NORELATIONSHIPRISKSECURITYALTID_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noRelationshipRiskSecurityAltID) {
			group = new RelationshipRiskSecAltIDGrp[noRelationshipRiskSecurityAltID];

			for ( int i = 0; i < noRelationshipRiskSecurityAltID; i++ ) group[i] = new RelationshipRiskSecAltIDGrp();
	}

		for ( int i = 0; i < noRelationshipRiskSecurityAltID; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noRelationshipRiskSecurityAltID; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noRelationshipRiskSecurityAltID; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noRelationshipRiskSecurityAltID; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixRelationshipRiskSecAltIDGrp)) return false;

		FixRelationshipRiskSecAltIDGrp msg = (FixRelationshipRiskSecAltIDGrp) o;

		for (int i = 0; i<noRelationshipRiskSecurityAltID; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noRelationshipRiskSecurityAltID; i++)
			s += group[i].toString();
		return s;
	}

public class RelationshipRiskSecAltIDGrp implements FixComponent
{

	public long noRelationshipRiskSecurityAltID = 0;
	public byte[] relationshipRiskSecurityAltID;
	public byte[] relationshipRiskSecurityAltIDSource;

	public RelationshipRiskSecAltIDGrp() {
		super();

		relationshipRiskSecurityAltID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		relationshipRiskSecurityAltIDSource = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		noRelationshipRiskSecurityAltID = Long.MAX_VALUE;		
		Utils.fill( relationshipRiskSecurityAltID, (byte)0 );
		Utils.fill( relationshipRiskSecurityAltIDSource, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.NORELATIONSHIPRISKSECURITYALTID_INT) {
				noRelationshipRiskSecurityAltID = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.RELATIONSHIPRISKSECURITYALTID_INT) {
				relationshipRiskSecurityAltID = FixUtils.getTagStringValue(value, relationshipRiskSecurityAltID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.RELATIONSHIPRISKSECURITYALTIDSOURCE_INT) {
				relationshipRiskSecurityAltIDSource = FixUtils.getTagStringValue(value, relationshipRiskSecurityAltIDSource);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(noRelationshipRiskSecurityAltID)) return true;
		if (FixUtils.isSet(relationshipRiskSecurityAltID)) return true;
		if (FixUtils.isSet(relationshipRiskSecurityAltIDSource)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(noRelationshipRiskSecurityAltID)) FixUtils.putFixTag( out, FixTags.NORELATIONSHIPRISKSECURITYALTID_INT, noRelationshipRiskSecurityAltID);
		if (FixUtils.isSet(relationshipRiskSecurityAltID)) FixUtils.putFixTag( out, FixTags.RELATIONSHIPRISKSECURITYALTID_INT, relationshipRiskSecurityAltID, 0, Utils.lastIndexTrim(relationshipRiskSecurityAltID, (byte)0) );
		if (FixUtils.isSet(relationshipRiskSecurityAltIDSource)) FixUtils.putFixTag( out, FixTags.RELATIONSHIPRISKSECURITYALTIDSOURCE_INT, relationshipRiskSecurityAltIDSource, 0, Utils.lastIndexTrim(relationshipRiskSecurityAltIDSource, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(noRelationshipRiskSecurityAltID)) s += "NoRelationshipRiskSecurityAltID(1593)=" + String.valueOf(noRelationshipRiskSecurityAltID) + sep;
			if (FixUtils.isSet(relationshipRiskSecurityAltID)) s += "RelationshipRiskSecurityAltID(1594)=" + new String(relationshipRiskSecurityAltID) + sep;
			if (FixUtils.isSet(relationshipRiskSecurityAltIDSource)) s += "RelationshipRiskSecurityAltIDSource(1595)=" + new String(relationshipRiskSecurityAltIDSource) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof RelationshipRiskSecAltIDGrp)) return false;

			RelationshipRiskSecAltIDGrp msg = (RelationshipRiskSecAltIDGrp) o;

		if (!( noRelationshipRiskSecurityAltID==msg.noRelationshipRiskSecurityAltID)) return false;

		if (!Utils.equals( relationshipRiskSecurityAltID, msg.relationshipRiskSecurityAltID)) return false;

		if (!Utils.equals( relationshipRiskSecurityAltIDSource, msg.relationshipRiskSecurityAltIDSource)) return false;

		return true;
	}
}
}
