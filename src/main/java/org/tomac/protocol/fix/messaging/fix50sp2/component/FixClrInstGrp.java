package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixClrInstGrp
{

	public int noClearingInstructions;
	public ClrInstGrp[] group;

	public void getAll(int noClearingInstructions, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noClearingInstructions = noClearingInstructions;

		if (noClearingInstructions < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noClearingInstructions ).getBytes(), FixTags.NOCLEARINGINSTRUCTIONS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noClearingInstructions) {
			group = new ClrInstGrp[noClearingInstructions];

			for ( int i = 0; i < noClearingInstructions; i++ ) group[i] = new ClrInstGrp();
	}

		for ( int i = 0; i < noClearingInstructions; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noClearingInstructions; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noClearingInstructions; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noClearingInstructions; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixClrInstGrp)) return false;

		FixClrInstGrp msg = (FixClrInstGrp) o;

		for (int i = 0; i<noClearingInstructions; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noClearingInstructions; i++)
			s += group[i].toString();
		return s;
	}

public class ClrInstGrp implements FixComponent
{

	public long clearingInstruction = 0;

	public ClrInstGrp() {
		super();

		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		clearingInstruction = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.CLEARINGINSTRUCTION_INT) {
				clearingInstruction = FixUtils.getTagIntValue( value );
				if (!FixMessageInfo.ClearingInstruction.isValid(clearingInstruction) ) throw new FixSessionException(SessionRejectReason.VALUE_IS_INCORRECT_OUT_OF_RANGE_FOR_THIS_TAG, ("Invalid enumerated value(" + clearingInstruction + ") for tag").getBytes(), id, new byte[0] );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(clearingInstruction)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(clearingInstruction)) FixUtils.putFixTag( out, FixTags.CLEARINGINSTRUCTION_INT, clearingInstruction);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(clearingInstruction)) s += "ClearingInstruction(577)=" + String.valueOf(clearingInstruction) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof ClrInstGrp)) return false;

			ClrInstGrp msg = (ClrInstGrp) o;

		if (!( clearingInstruction==msg.clearingInstruction)) return false;

		return true;
	}
}
}
