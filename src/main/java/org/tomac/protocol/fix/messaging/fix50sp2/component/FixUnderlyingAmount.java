package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixUnderlyingAmount
{

	public int noUnderlyingAmounts;
	public UnderlyingAmount[] group;

	public void getAll(int noUnderlyingAmounts, ByteBuffer buf) throws FixSessionException {
		this.noUnderlyingAmounts = noUnderlyingAmounts;

		if (noUnderlyingAmounts < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noUnderlyingAmounts ).getBytes(), FixTags.NOUNDERLYINGAMOUNTS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noUnderlyingAmounts) {
			group = new UnderlyingAmount[noUnderlyingAmounts];

			for ( int i = 0; i < noUnderlyingAmounts; i++ ) group[i] = new UnderlyingAmount();
	}

		for ( int i = 0; i < noUnderlyingAmounts; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noUnderlyingAmounts; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noUnderlyingAmounts; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noUnderlyingAmounts; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noUnderlyingAmounts; i++)
			s += group[i].toString();
		return s;
	}

public class UnderlyingAmount implements FixComponent
{

	public long underlyingPayAmount = 0;
	public long underlyingCollectAmount = 0;
	public byte[] underlyingSettlementDate;
	public byte[] underlyingSettlementStatus;

	public UnderlyingAmount() {
		super();

		underlyingSettlementDate = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		underlyingSettlementStatus = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		underlyingPayAmount = Long.MAX_VALUE;		
		underlyingCollectAmount = Long.MAX_VALUE;		
		Utils.fill( underlyingSettlementDate, (byte)0 );
		Utils.fill( underlyingSettlementStatus, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.UNDERLYINGPAYAMOUNT_INT) {
				underlyingPayAmount = FixUtils.getTagFloatValue(value);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.UNDERLYINGCOLLECTAMOUNT_INT) {
				underlyingCollectAmount = FixUtils.getTagFloatValue(value);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.UNDERLYINGSETTLEMENTDATE_INT) {
				underlyingSettlementDate = FixUtils.getTagStringValue(value, underlyingSettlementDate);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.UNDERLYINGSETTLEMENTSTATUS_INT) {
				underlyingSettlementStatus = FixUtils.getTagStringValue(value, underlyingSettlementStatus);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(underlyingPayAmount)) return true;
		if (FixUtils.isSet(underlyingCollectAmount)) return true;
		if (FixUtils.isSet(underlyingSettlementDate)) return true;
		if (FixUtils.isSet(underlyingSettlementStatus)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(underlyingPayAmount)) FixUtils.putFixTag( out, FixTags.UNDERLYINGPAYAMOUNT_INT, underlyingPayAmount);
		if (FixUtils.isSet(underlyingCollectAmount)) FixUtils.putFixTag( out, FixTags.UNDERLYINGCOLLECTAMOUNT_INT, underlyingCollectAmount);
		if (FixUtils.isSet(underlyingSettlementDate)) FixUtils.putFixTag( out, FixTags.UNDERLYINGSETTLEMENTDATE_INT, underlyingSettlementDate);
		if (FixUtils.isSet(underlyingSettlementStatus)) FixUtils.putFixTag( out, FixTags.UNDERLYINGSETTLEMENTSTATUS_INT, underlyingSettlementStatus, 0, Utils.lastIndexTrim(underlyingSettlementStatus, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(underlyingPayAmount)) s += "UnderlyingPayAmount(985)=" + String.valueOf(underlyingPayAmount) + sep;
			if (FixUtils.isSet(underlyingCollectAmount)) s += "UnderlyingCollectAmount(986)=" + String.valueOf(underlyingCollectAmount) + sep;
			if (FixUtils.isSet(underlyingSettlementDate)) s += "UnderlyingSettlementDate(987)=" + new String(underlyingSettlementDate) + sep;
			if (FixUtils.isSet(underlyingSettlementStatus)) s += "UnderlyingSettlementStatus(988)=" + new String(underlyingSettlementStatus) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof UnderlyingAmount)) return false;

			UnderlyingAmount msg = (UnderlyingAmount) o;

		if ( ! super.equals(msg) ) return false;

		if (!( underlyingPayAmount==msg.underlyingPayAmount)) return false;

		if (!( underlyingCollectAmount==msg.underlyingCollectAmount)) return false;

		if (!Utils.equals( underlyingSettlementStatus, msg.underlyingSettlementStatus)) return false;

		return true;
	}
}
}
