package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.*;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixRateSource
{

	public int noRateSources;
	public RateSource[] group;

	public void getAll(int noRateSources, ByteBuffer buf) throws FixSessionException {
		this.noRateSources = noRateSources;

		if (noRateSources < 1) throw new FixSessionException("asdasd");
		// this will leak memory if we grow the group
		if (group.length < noRateSources) 
			group = new RateSource[noRateSources];

		for ( int i = 0; i < noRateSources; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noRateSources; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noRateSources; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noRateSources; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noRateSources; i++)
			s += group[i].toString();
		return s;
	}

public class RateSource implements FixComponent
{

	public long rateSource = 0;
	public long rateSourceType = 0;
	public byte[] referencePage;

	public RateSource() {
		super();

		referencePage = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		rateSource = Long.MAX_VALUE;		
		rateSourceType = Long.MAX_VALUE;		
		Utils.fill( referencePage, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.RATESOURCE_INT) {
				rateSource = FixUtils.getTagIntValue( value );
				if (!RateSource.isValid(rateSource) ) throw new FixSessionException(buf, "Invalid enumerated value(" + rateSource + ") for tag: " + id );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.RATESOURCETYPE_INT) {
				rateSourceType = FixUtils.getTagIntValue( value );
				if (!RateSourceType.isValid(rateSourceType) ) throw new FixSessionException(buf, "Invalid enumerated value(" + rateSourceType + ") for tag: " + id );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.REFERENCEPAGE_INT) {
				referencePage = FixUtils.getTagStringValue(value, referencePage);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
			if (id > 0) throw new FixSessionException(buf, "Required tag missing: " + id );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(rateSource)) return true;
		if (FixUtils.isSet(rateSourceType)) return true;
		if (FixUtils.isSet(referencePage)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(rateSource)) FixUtils.putFixTag( out, FixTags.RATESOURCE_INT, rateSource);
		if (FixUtils.isSet(rateSourceType)) FixUtils.putFixTag( out, FixTags.RATESOURCETYPE_INT, rateSourceType);
		if (FixUtils.isSet(referencePage)) FixUtils.putFixTag( out, FixTags.REFERENCEPAGE_INT, referencePage, 0, Utils.lastIndexTrim(referencePage, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(rateSource)) s += "RateSource(1446)=" + String.valueOf(rateSource) + sep;
			if (FixUtils.isSet(rateSourceType)) s += "RateSourceType(1447)=" + String.valueOf(rateSourceType) + sep;
			if (FixUtils.isSet(referencePage)) s += "ReferencePage(1448)=" + new String(referencePage) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof RateSource)) return false;

			RateSource msg = (RateSource) o;

		if ( ! super.equals(msg) ) return false;

		if (!( rateSource==msg.rateSource)) return false;

		if (!( rateSourceType==msg.rateSourceType)) return false;

		if (!Utils.equals( referencePage, msg.referencePage)) return false;

		return true;
	}
}
}
