package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;
import org.tomac.protocol.fix.messaging.fix50sp2.component.FixRootSubParties;

public class FixRootParties
{

	public int noRootPartyIDs;
	public RootParties[] group;

	public void getAll(int noRootPartyIDs, ByteBuffer buf) throws FixSessionException {
		this.noRootPartyIDs = noRootPartyIDs;

		if (noRootPartyIDs < 1) throw new FixSessionException("asdasd");
		// this will leak memory if we grow the group
		if (group == null || group.length < noRootPartyIDs) {
			group = new RootParties[noRootPartyIDs];

			for ( int i = 0; i < noRootPartyIDs; i++ ) group[i] = new RootParties();
	}

		for ( int i = 0; i < noRootPartyIDs; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noRootPartyIDs; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noRootPartyIDs; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noRootPartyIDs; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noRootPartyIDs; i++)
			s += group[i].toString();
		return s;
	}

public class RootParties implements FixComponent
{

	public byte[] rootPartyID;
	public byte rootPartyIDSource = (byte)' ';
	public long rootPartyRole = 0;
	public FixRootSubParties rootSubParties;

	public RootParties() {
		super();

		rootPartyID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		rootSubParties = new FixRootSubParties();
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( rootPartyID, (byte)0 );
		rootPartyIDSource = Byte.MAX_VALUE;		
		rootPartyRole = Long.MAX_VALUE;		
		rootSubParties.clear();
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.ROOTPARTYID_INT) {
				rootPartyID = FixUtils.getTagStringValue(value, rootPartyID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.ROOTPARTYIDSOURCE_INT) {
				rootPartyIDSource = FixUtils.getTagCharValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.ROOTPARTYROLE_INT) {
				rootPartyRole = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.NOROOTPARTYSUBIDS_INT) {
				rootSubParties.getAll(FixTags.NOROOTPARTYSUBIDS_INT, buf);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
			if (id > 0) throw new FixSessionException(buf, "Required tag missing: " + id );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(rootPartyID)) return true;
		if (FixUtils.isSet(rootPartyIDSource)) return true;
		if (FixUtils.isSet(rootPartyRole)) return true;
		if (FixUtils.isSet(rootSubParties.noRootPartySubIDs)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(rootPartyID)) FixUtils.putFixTag( out, FixTags.ROOTPARTYID_INT, rootPartyID, 0, Utils.lastIndexTrim(rootPartyID, (byte)0) );
		if (FixUtils.isSet(rootPartyIDSource)) FixUtils.putFixTag( out, FixTags.ROOTPARTYIDSOURCE_INT, rootPartyIDSource );
		if (FixUtils.isSet(rootPartyRole)) FixUtils.putFixTag( out, FixTags.ROOTPARTYROLE_INT, rootPartyRole);
		if (FixUtils.isSet(rootSubParties.noRootPartySubIDs)) rootSubParties.encode( out );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(rootPartyID)) s += "RootPartyID(1117)=" + new String(rootPartyID) + sep;
			if (FixUtils.isSet(rootPartyIDSource)) s += "RootPartyIDSource(1118)=" + String.valueOf(rootPartyIDSource) + sep;
			if (FixUtils.isSet(rootPartyRole)) s += "RootPartyRole(1119)=" + String.valueOf(rootPartyRole) + sep;
			if (FixUtils.isSet(rootSubParties.noRootPartySubIDs)) s += rootSubParties.toString();
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof RootParties)) return false;

			RootParties msg = (RootParties) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( rootPartyID, msg.rootPartyID)) return false;

		if (!( rootPartyIDSource==msg.rootPartyIDSource)) return false;

		if (!( rootPartyRole==msg.rootPartyRole)) return false;

		if (!rootSubParties.equals(msg.rootSubParties)) return false;

		return true;
	}
}
}
