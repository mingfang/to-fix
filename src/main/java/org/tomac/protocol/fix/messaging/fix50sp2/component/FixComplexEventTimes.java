package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixComplexEventTimes
{

	public int noComplexEventTimes;
	public ComplexEventTimes[] group;

	public void getAll(int noComplexEventTimes, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noComplexEventTimes = noComplexEventTimes;

		if (noComplexEventTimes < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noComplexEventTimes ).getBytes(), FixTags.NOCOMPLEXEVENTTIMES_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noComplexEventTimes) {
			group = new ComplexEventTimes[noComplexEventTimes];

			for ( int i = 0; i < noComplexEventTimes; i++ ) group[i] = new ComplexEventTimes();
	}

		for ( int i = 0; i < noComplexEventTimes; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noComplexEventTimes; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noComplexEventTimes; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noComplexEventTimes; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixComplexEventTimes)) return false;

		FixComplexEventTimes msg = (FixComplexEventTimes) o;

		for (int i = 0; i<noComplexEventTimes; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noComplexEventTimes; i++)
			s += group[i].toString();
		return s;
	}

public class ComplexEventTimes implements FixComponent
{

	public byte[] complexEventStartTime;
	public byte[] complexEventEndTime;

	public ComplexEventTimes() {
		super();

		complexEventStartTime = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		complexEventEndTime = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( complexEventStartTime, (byte)0 );
		Utils.fill( complexEventEndTime, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.COMPLEXEVENTSTARTTIME_INT) {
				complexEventStartTime = FixUtils.getTagStringValue(value, complexEventStartTime);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.COMPLEXEVENTENDTIME_INT) {
				complexEventEndTime = FixUtils.getTagStringValue(value, complexEventEndTime);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(complexEventStartTime)) return true;
		if (FixUtils.isSet(complexEventEndTime)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(complexEventStartTime)) FixUtils.putFixTag( out, FixTags.COMPLEXEVENTSTARTTIME_INT, complexEventStartTime);
		if (FixUtils.isSet(complexEventEndTime)) FixUtils.putFixTag( out, FixTags.COMPLEXEVENTENDTIME_INT, complexEventEndTime);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(complexEventStartTime)) s += "ComplexEventStartTime(1495)=" + new String(complexEventStartTime) + sep;
			if (FixUtils.isSet(complexEventEndTime)) s += "ComplexEventEndTime(1496)=" + new String(complexEventEndTime) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof ComplexEventTimes)) return false;

			ComplexEventTimes msg = (ComplexEventTimes) o;

		return true;
	}
}
}
