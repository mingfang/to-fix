package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixAltPtysSubGrp
{

	public int noPartyAltSubIDs;
	public AltPtysSubGrp[] group;

	public void getAll(int noPartyAltSubIDs, ByteBuffer buf) throws FixSessionException {
		this.noPartyAltSubIDs = noPartyAltSubIDs;

		if (noPartyAltSubIDs < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noPartyAltSubIDs ).getBytes(), FixTags.NOPARTYALTSUBIDS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noPartyAltSubIDs) {
			group = new AltPtysSubGrp[noPartyAltSubIDs];

			for ( int i = 0; i < noPartyAltSubIDs; i++ ) group[i] = new AltPtysSubGrp();
	}

		for ( int i = 0; i < noPartyAltSubIDs; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noPartyAltSubIDs; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noPartyAltSubIDs; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noPartyAltSubIDs; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noPartyAltSubIDs; i++)
			s += group[i].toString();
		return s;
	}

public class AltPtysSubGrp implements FixComponent
{

	public byte[] partyAltSubID;
	public long partyAltSubIDType = 0;

	public AltPtysSubGrp() {
		super();

		partyAltSubID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( partyAltSubID, (byte)0 );
		partyAltSubIDType = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.PARTYALTSUBID_INT) {
				partyAltSubID = FixUtils.getTagStringValue(value, partyAltSubID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.PARTYALTSUBIDTYPE_INT) {
				partyAltSubIDType = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(partyAltSubID)) return true;
		if (FixUtils.isSet(partyAltSubIDType)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(partyAltSubID)) FixUtils.putFixTag( out, FixTags.PARTYALTSUBID_INT, partyAltSubID, 0, Utils.lastIndexTrim(partyAltSubID, (byte)0) );
		if (FixUtils.isSet(partyAltSubIDType)) FixUtils.putFixTag( out, FixTags.PARTYALTSUBIDTYPE_INT, partyAltSubIDType);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(partyAltSubID)) s += "PartyAltSubID(1520)=" + new String(partyAltSubID) + sep;
			if (FixUtils.isSet(partyAltSubIDType)) s += "PartyAltSubIDType(1521)=" + String.valueOf(partyAltSubIDType) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof AltPtysSubGrp)) return false;

			AltPtysSubGrp msg = (AltPtysSubGrp) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( partyAltSubID, msg.partyAltSubID)) return false;

		if (!( partyAltSubIDType==msg.partyAltSubIDType)) return false;

		return true;
	}
}
}
