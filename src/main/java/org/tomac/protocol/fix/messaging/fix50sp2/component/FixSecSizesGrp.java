package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixSecSizesGrp
{

	public int noOfSecSizes;
	public SecSizesGrp[] group;

	public void getAll(int noOfSecSizes, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noOfSecSizes = noOfSecSizes;

		if (noOfSecSizes < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noOfSecSizes ).getBytes(), FixTags.NOOFSECSIZES_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noOfSecSizes) {
			group = new SecSizesGrp[noOfSecSizes];

			for ( int i = 0; i < noOfSecSizes; i++ ) group[i] = new SecSizesGrp();
	}

		for ( int i = 0; i < noOfSecSizes; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noOfSecSizes; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noOfSecSizes; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noOfSecSizes; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixSecSizesGrp)) return false;

		FixSecSizesGrp msg = (FixSecSizesGrp) o;

		for (int i = 0; i<noOfSecSizes; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noOfSecSizes; i++)
			s += group[i].toString();
		return s;
	}

public class SecSizesGrp implements FixComponent
{

	public long mDSecSizeType = 0;
	public long mDSecSize = 0;

	public SecSizesGrp() {
		super();

		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		mDSecSizeType = Long.MAX_VALUE;		
		mDSecSize = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.MDSECSIZETYPE_INT) {
				mDSecSizeType = FixUtils.getTagIntValue(null ,id ,value );
				if (!FixMessageInfo.MDSecSizeType.isValid(mDSecSizeType) ) throw new FixSessionException(SessionRejectReason.VALUE_IS_INCORRECT_OUT_OF_RANGE_FOR_THIS_TAG, ("Invalid enumerated value(" + mDSecSizeType + ") for tag").getBytes(), id, new byte[0] );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.MDSECSIZE_INT) {
				mDSecSize = FixUtils.getTagFloatValue(null ,id ,value);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(mDSecSizeType)) return true;
		if (FixUtils.isSet(mDSecSize)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(mDSecSizeType)) FixUtils.putFixTag( out, FixTags.MDSECSIZETYPE_INT, mDSecSizeType);
		if (FixUtils.isSet(mDSecSize)) FixUtils.putFixFloatTag( out, FixTags.MDSECSIZE_INT, mDSecSize);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(mDSecSizeType)) s += "MDSecSizeType(1178)=" + String.valueOf(mDSecSizeType) + sep;
			if (FixUtils.isSet(mDSecSize)) s += "MDSecSize(1179)=" + String.valueOf(mDSecSize) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof SecSizesGrp)) return false;

			SecSizesGrp msg = (SecSizesGrp) o;

		if (!( mDSecSizeType==msg.mDSecSizeType)) return false;

		if (!( mDSecSize==msg.mDSecSize)) return false;

		return true;
	}
}
}
