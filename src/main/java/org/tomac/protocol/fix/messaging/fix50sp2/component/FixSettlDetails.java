package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;
import org.tomac.protocol.fix.messaging.fix50sp2.component.FixSettlParties;

public class FixSettlDetails
{

	public int noSettlDetails;
	public SettlDetails[] group;

	public void getAll(int noSettlDetails, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noSettlDetails = noSettlDetails;

		if (noSettlDetails < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noSettlDetails ).getBytes(), FixTags.NOSETTLDETAILS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noSettlDetails) {
			group = new SettlDetails[noSettlDetails];

			for ( int i = 0; i < noSettlDetails; i++ ) group[i] = new SettlDetails();
	}

		for ( int i = 0; i < noSettlDetails; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noSettlDetails; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noSettlDetails; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noSettlDetails; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixSettlDetails)) return false;

		FixSettlDetails msg = (FixSettlDetails) o;

		for (int i = 0; i<noSettlDetails; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noSettlDetails; i++)
			s += group[i].toString();
		return s;
	}

public class SettlDetails implements FixComponent
{

	public byte settlObligSource = (byte)' ';
	public FixSettlParties settlParties;

	public SettlDetails() {
		super();

		settlParties = new FixSettlParties();
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		settlObligSource = Byte.MAX_VALUE;		
		settlParties.clear();
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.SETTLOBLIGSOURCE_INT) {
				settlObligSource = FixUtils.getTagCharValue(null ,id ,value );
				if (!FixMessageInfo.SettlObligSource.isValid(settlObligSource) ) throw new FixSessionException(SessionRejectReason.VALUE_IS_INCORRECT_OUT_OF_RANGE_FOR_THIS_TAG, ("Invalid enumerated value(" + settlObligSource + ") for tag").getBytes(), id, new byte[0] );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.NOSETTLPARTYIDS_INT) {
				int noSettlPartyIDs;
				noSettlPartyIDs = FixUtils.getTagIntValue(null ,id ,value );
				settlParties.getAll(noSettlPartyIDs, buf);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(settlObligSource)) return true;
		if (FixUtils.isSet(settlParties.noSettlPartyIDs)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(settlObligSource)) FixUtils.putFixTag( out, FixTags.SETTLOBLIGSOURCE_INT, settlObligSource );
		if (FixUtils.isSet(settlParties.noSettlPartyIDs)) settlParties.encode( out );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(settlObligSource)) s += "SettlObligSource(1164)=" + String.valueOf(settlObligSource) + sep;
			if (FixUtils.isSet(settlParties.noSettlPartyIDs)) s += settlParties.toString();
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof SettlDetails)) return false;

			SettlDetails msg = (SettlDetails) o;

		if (!( settlObligSource==msg.settlObligSource)) return false;

		if (!settlParties.equals(msg.settlParties)) return false;

		return true;
	}
}
}
