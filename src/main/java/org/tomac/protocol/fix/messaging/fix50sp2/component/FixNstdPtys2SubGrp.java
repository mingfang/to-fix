package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixNstdPtys2SubGrp
{

	public int noNested2PartySubIDs;
	public NstdPtys2SubGrp[] group;

	public void getAll(int noNested2PartySubIDs, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noNested2PartySubIDs = noNested2PartySubIDs;

		if (noNested2PartySubIDs < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noNested2PartySubIDs ).getBytes(), FixTags.NONESTED2PARTYSUBIDS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noNested2PartySubIDs) {
			group = new NstdPtys2SubGrp[noNested2PartySubIDs];

			for ( int i = 0; i < noNested2PartySubIDs; i++ ) group[i] = new NstdPtys2SubGrp();
	}

		for ( int i = 0; i < noNested2PartySubIDs; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noNested2PartySubIDs; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noNested2PartySubIDs; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noNested2PartySubIDs; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noNested2PartySubIDs; i++)
			s += group[i].toString();
		return s;
	}

public class NstdPtys2SubGrp implements FixComponent
{

	public byte[] nested2PartySubID;
	public long nested2PartySubIDType = 0;

	public NstdPtys2SubGrp() {
		super();

		nested2PartySubID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( nested2PartySubID, (byte)0 );
		nested2PartySubIDType = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.NESTED2PARTYSUBID_INT) {
				nested2PartySubID = FixUtils.getTagStringValue(value, nested2PartySubID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.NESTED2PARTYSUBIDTYPE_INT) {
				nested2PartySubIDType = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(nested2PartySubID)) return true;
		if (FixUtils.isSet(nested2PartySubIDType)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(nested2PartySubID)) FixUtils.putFixTag( out, FixTags.NESTED2PARTYSUBID_INT, nested2PartySubID, 0, Utils.lastIndexTrim(nested2PartySubID, (byte)0) );
		if (FixUtils.isSet(nested2PartySubIDType)) FixUtils.putFixTag( out, FixTags.NESTED2PARTYSUBIDTYPE_INT, nested2PartySubIDType);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(nested2PartySubID)) s += "Nested2PartySubID(760)=" + new String(nested2PartySubID) + sep;
			if (FixUtils.isSet(nested2PartySubIDType)) s += "Nested2PartySubIDType(807)=" + String.valueOf(nested2PartySubIDType) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof NstdPtys2SubGrp)) return false;

			NstdPtys2SubGrp msg = (NstdPtys2SubGrp) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( nested2PartySubID, msg.nested2PartySubID)) return false;

		if (!( nested2PartySubIDType==msg.nested2PartySubIDType)) return false;

		return true;
	}
}
}
