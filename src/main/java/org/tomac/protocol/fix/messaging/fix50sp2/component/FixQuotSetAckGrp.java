package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;
import org.tomac.protocol.fix.messaging.fix50sp2.component.FixUnderlyingInstrument;
import org.tomac.protocol.fix.messaging.fix50sp2.component.FixQuotEntryAckGrp;

public class FixQuotSetAckGrp
{

	public int noQuoteSets;
	public QuotSetAckGrp[] group;

	public void getAll(int noQuoteSets, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noQuoteSets = noQuoteSets;

		if (noQuoteSets < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noQuoteSets ).getBytes(), FixTags.NOQUOTESETS_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noQuoteSets) {
			group = new QuotSetAckGrp[noQuoteSets];

			for ( int i = 0; i < noQuoteSets; i++ ) group[i] = new QuotSetAckGrp();
	}

		for ( int i = 0; i < noQuoteSets; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noQuoteSets; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noQuoteSets; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noQuoteSets; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixQuotSetAckGrp)) return false;

		FixQuotSetAckGrp msg = (FixQuotSetAckGrp) o;

		for (int i = 0; i<noQuoteSets; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noQuoteSets; i++)
			s += group[i].toString();
		return s;
	}

public class QuotSetAckGrp implements FixComponent
{

	public byte[] quoteSetID;
	public byte[] quoteSetValidUntilTime;
	public FixUnderlyingInstrument underlyingInstrument;
	public long totNoQuoteEntries = 0;
	public long totNoCxldQuotes = 0;
	public long totNoAccQuotes = 0;
	public long totNoRejQuotes = 0;
	public boolean lastFragment = false;
	public FixQuotEntryAckGrp quotEntryAckGrp;

	public QuotSetAckGrp() {
		super();

		quoteSetID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		quoteSetValidUntilTime = new byte[FixUtils.UTCTIMESTAMP_LENGTH];
		underlyingInstrument = new FixUnderlyingInstrument();
		quotEntryAckGrp = new FixQuotEntryAckGrp();
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( quoteSetID, (byte)0 );
		Utils.fill( quoteSetValidUntilTime, (byte)0 );
		totNoQuoteEntries = Long.MAX_VALUE;		
		totNoCxldQuotes = Long.MAX_VALUE;		
		totNoAccQuotes = Long.MAX_VALUE;		
		totNoRejQuotes = Long.MAX_VALUE;		
		lastFragment = false;		
		underlyingInstrument.clear();
		quotEntryAckGrp.clear();
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.QUOTESETID_INT) {
				quoteSetID = FixUtils.getTagStringValue(value, quoteSetID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.QUOTESETVALIDUNTILTIME_INT) {
				quoteSetValidUntilTime = FixUtils.getTagStringValue(value, quoteSetValidUntilTime);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.UNDERLYINGSYMBOL_INT) {
				underlyingInstrument.getAll(FixTags.UNDERLYINGSYMBOL_INT, buf);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.TOTNOQUOTEENTRIES_INT) {
				totNoQuoteEntries = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.TOTNOCXLDQUOTES_INT) {
				totNoCxldQuotes = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.TOTNOACCQUOTES_INT) {
				totNoAccQuotes = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.TOTNOREJQUOTES_INT) {
				totNoRejQuotes = FixUtils.getTagIntValue( value );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.LASTFRAGMENT_INT) {
				lastFragment = FixUtils.getTagBooleanValue( value );
				if (!FixMessageInfo.LastFragment.isValid(lastFragment) ) throw new FixSessionException(SessionRejectReason.VALUE_IS_INCORRECT_OUT_OF_RANGE_FOR_THIS_TAG, ("Invalid enumerated value(" + lastFragment + ") for tag").getBytes(), id, new byte[0] );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.NOQUOTEENTRIES_INT) {
				int noQuoteEntries;
				noQuoteEntries = FixUtils.getTagIntValue( value );
				quotEntryAckGrp.getAll(noQuoteEntries, buf);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(quoteSetID)) return true;
		if (FixUtils.isSet(quoteSetValidUntilTime)) return true;
		if (FixUtils.isSet(underlyingInstrument.underlyingSymbol)) return true;
		if (FixUtils.isSet(totNoQuoteEntries)) return true;
		if (FixUtils.isSet(totNoCxldQuotes)) return true;
		if (FixUtils.isSet(totNoAccQuotes)) return true;
		if (FixUtils.isSet(totNoRejQuotes)) return true;
		if (FixUtils.isSet(lastFragment)) return true;
		if (FixUtils.isSet(quotEntryAckGrp.noQuoteEntries)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(quoteSetID)) FixUtils.putFixTag( out, FixTags.QUOTESETID_INT, quoteSetID, 0, Utils.lastIndexTrim(quoteSetID, (byte)0) );
		if (FixUtils.isSet(quoteSetValidUntilTime)) FixUtils.putFixTag( out, FixTags.QUOTESETVALIDUNTILTIME_INT, quoteSetValidUntilTime);
		if (FixUtils.isSet(underlyingInstrument.underlyingSymbol)) underlyingInstrument.encode( out );
		if (FixUtils.isSet(totNoQuoteEntries)) FixUtils.putFixTag( out, FixTags.TOTNOQUOTEENTRIES_INT, totNoQuoteEntries);
		if (FixUtils.isSet(totNoCxldQuotes)) FixUtils.putFixTag( out, FixTags.TOTNOCXLDQUOTES_INT, totNoCxldQuotes);
		if (FixUtils.isSet(totNoAccQuotes)) FixUtils.putFixTag( out, FixTags.TOTNOACCQUOTES_INT, totNoAccQuotes);
		if (FixUtils.isSet(totNoRejQuotes)) FixUtils.putFixTag( out, FixTags.TOTNOREJQUOTES_INT, totNoRejQuotes);
		if (FixUtils.isSet(lastFragment)) FixUtils.putFixTag( out, FixTags.LASTFRAGMENT_INT, lastFragment?(byte)'Y':(byte)'N' );
		if (FixUtils.isSet(quotEntryAckGrp.noQuoteEntries)) quotEntryAckGrp.encode( out );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(quoteSetID)) s += "QuoteSetID(302)=" + new String(quoteSetID) + sep;
			if (FixUtils.isSet(quoteSetValidUntilTime)) s += "QuoteSetValidUntilTime(367)=" + new String(quoteSetValidUntilTime) + sep;
			if (FixUtils.isSet(underlyingInstrument.underlyingSymbol)) s += underlyingInstrument.toString();
			if (FixUtils.isSet(totNoQuoteEntries)) s += "TotNoQuoteEntries(304)=" + String.valueOf(totNoQuoteEntries) + sep;
			if (FixUtils.isSet(totNoCxldQuotes)) s += "TotNoCxldQuotes(1168)=" + String.valueOf(totNoCxldQuotes) + sep;
			if (FixUtils.isSet(totNoAccQuotes)) s += "TotNoAccQuotes(1169)=" + String.valueOf(totNoAccQuotes) + sep;
			if (FixUtils.isSet(totNoRejQuotes)) s += "TotNoRejQuotes(1170)=" + String.valueOf(totNoRejQuotes) + sep;
			if (FixUtils.isSet(lastFragment)) s += "LastFragment(893)=" + String.valueOf(lastFragment) + sep;
			if (FixUtils.isSet(quotEntryAckGrp.noQuoteEntries)) s += quotEntryAckGrp.toString();
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof QuotSetAckGrp)) return false;

			QuotSetAckGrp msg = (QuotSetAckGrp) o;

		if (!Utils.equals( quoteSetID, msg.quoteSetID)) return false;

		if (!underlyingInstrument.equals(msg.underlyingInstrument)) return false;

		if (!( totNoQuoteEntries==msg.totNoQuoteEntries)) return false;

		if (!( totNoCxldQuotes==msg.totNoCxldQuotes)) return false;

		if (!( totNoAccQuotes==msg.totNoAccQuotes)) return false;

		if (!( totNoRejQuotes==msg.totNoRejQuotes)) return false;

		if (!( lastFragment==msg.lastFragment)) return false;

		if (!quotEntryAckGrp.equals(msg.quotEntryAckGrp)) return false;

		return true;
	}
}
}
