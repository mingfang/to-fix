package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixNewsRefGrp
{

	public int noNewsRefIDs;
	public NewsRefGrp[] group;

	public void getAll(int noNewsRefIDs, ByteBuffer buf) throws FixSessionException {
		this.noNewsRefIDs = noNewsRefIDs;

		if (noNewsRefIDs < 1) throw new FixSessionException("asdasd");
		// this will leak memory if we grow the group
		if (group.length < noNewsRefIDs) 
			group = new NewsRefGrp[noNewsRefIDs];

		for ( int i = 0; i < noNewsRefIDs; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noNewsRefIDs; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noNewsRefIDs; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noNewsRefIDs; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noNewsRefIDs; i++)
			s += group[i].toString();
		return s;
	}

public class NewsRefGrp implements FixComponent
{

	public byte[] newsRefID;
	public long newsRefType = 0;

	public NewsRefGrp() {
		super();

		newsRefID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( newsRefID, (byte)0 );
		newsRefType = Long.MAX_VALUE;		
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.NEWSREFID_INT) {
				newsRefID = FixUtils.getTagStringValue(value, newsRefID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.NEWSREFTYPE_INT) {
				newsRefType = FixUtils.getTagIntValue( value );
				if (!FixMessageInfo.NewsRefType.isValid(newsRefType) ) throw new FixSessionException(buf, "Invalid enumerated value(" + newsRefType + ") for tag: " + id );
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
			if (id > 0) throw new FixSessionException(buf, "Required tag missing: " + id );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(newsRefID)) return true;
		if (FixUtils.isSet(newsRefType)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(newsRefID)) FixUtils.putFixTag( out, FixTags.NEWSREFID_INT, newsRefID, 0, Utils.lastIndexTrim(newsRefID, (byte)0) );
		if (FixUtils.isSet(newsRefType)) FixUtils.putFixTag( out, FixTags.NEWSREFTYPE_INT, newsRefType);
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(newsRefID)) s += "NewsRefID(1476)=" + new String(newsRefID) + sep;
			if (FixUtils.isSet(newsRefType)) s += "NewsRefType(1477)=" + String.valueOf(newsRefType) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof NewsRefGrp)) return false;

			NewsRefGrp msg = (NewsRefGrp) o;

		if ( ! super.equals(msg) ) return false;

		if (!Utils.equals( newsRefID, msg.newsRefID)) return false;

		if (!( newsRefType==msg.newsRefType)) return false;

		return true;
	}
}
}
