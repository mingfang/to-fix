package org.tomac.protocol.fix.messaging.fix50sp2.component;

// DO NOT EDIT!!!
// This file is generated by FixMessageGenerator.
// If you need additional functionality, put it in a helper class
// that does not live in this folder!!!  Any java file in this folder 
// will be deleted upon the next run of the FixMessageGenerator!

import java.nio.ByteBuffer;

import org.tomac.protocol.fix.FixUtils;
import org.tomac.protocol.fix.FixSessionException;
import org.tomac.protocol.fix.FixGarbledException;
import org.tomac.utils.Utils;
import org.tomac.protocol.fix.FixConstants;


import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo.SessionRejectReason;
import org.tomac.protocol.fix.messaging.fix50sp2.FixMessageInfo;
import org.tomac.protocol.fix.messaging.fix50sp2.FixTags;

public class FixSecAltIDGrp
{

	public int noSecurityAltID;
	public SecAltIDGrp[] group;

	public void getAll(int noSecurityAltID, ByteBuffer buf) throws FixSessionException, FixGarbledException {
		this.noSecurityAltID = noSecurityAltID;

		if (noSecurityAltID < 1) throw new FixSessionException(SessionRejectReason.INCORRECT_NUMINGROUP_COUNT_FOR_REPEATING_GROUP, ("Incorrect num in group count " + noSecurityAltID ).getBytes(), FixTags.NOSECURITYALTID_INT, new byte[0]);
		// this will leak memory if we grow the group
		if (group == null || group.length < noSecurityAltID) {
			group = new SecAltIDGrp[noSecurityAltID];

			for ( int i = 0; i < noSecurityAltID; i++ ) group[i] = new SecAltIDGrp();
	}

		for ( int i = 0; i < noSecurityAltID; i++ ) 
			group[i].getAllGroup(buf);
	}

	public void clear() {
		for (int i = 0; i<noSecurityAltID; i++)
			group[i].clear();
	}
	public void encode(ByteBuffer out) {
		for (int i = 0; i<noSecurityAltID; i++)
			group[i].encode(out);
	}
	public boolean isSet() {
		for (int i = 0; i<noSecurityAltID; i++)
			if (group[i].isSet()) return true;
		return false;
	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof FixSecAltIDGrp)) return false;

		FixSecAltIDGrp msg = (FixSecAltIDGrp) o;

		for (int i = 0; i<noSecurityAltID; i++)
			if (!group[i].equals(msg.group[i])) return false;
		return true;
	}

	@Override
	public String toString() {
		String s = "";
		for (int i = 0; i<noSecurityAltID; i++)
			s += group[i].toString();
		return s;
	}

public class SecAltIDGrp implements FixComponent
{

	public byte[] securityAltID;
	public byte[] securityAltIDSource;

	public SecAltIDGrp() {
		super();

		securityAltID = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		securityAltIDSource = new byte[FixUtils.FIX_MAX_STRING_LENGTH];
		this.clear();

	}

	@Override
	public void clear()
	{

		// clear out all the fields that aren't msgType

		Utils.fill( securityAltID, (byte)0 );
		Utils.fill( securityAltIDSource, (byte)0 );
	}

	public void getAllGroup(ByteBuffer buf) throws FixSessionException, FixGarbledException
	{

		int startTagPosition = buf.position();

		int id = FixUtils.getTagId( buf );
		int lastTagPosition = buf.position();
			ByteBuffer value;

			value = buf;

			if(id == FixTags.SECURITYALTID_INT) {
				securityAltID = FixUtils.getTagStringValue(value, securityAltID);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			if(id == FixTags.SECURITYALTIDSOURCE_INT) {
				securityAltIDSource = FixUtils.getTagStringValue(value, securityAltIDSource);
				lastTagPosition = buf.position();

				id = FixUtils.getTagId( buf );
			}

			id = checkRequiredTags();
				if (id > 0) throw new FixSessionException(SessionRejectReason.REQUIRED_TAG_MISSING, "Required tag missing".getBytes(), id, new byte[0] );

			buf.position( lastTagPosition );
			return;

	}

	private int checkRequiredTags() {
		int tag = -1;

		return tag;

	}
	@Override
	public boolean isSet()
	{
		if (FixUtils.isSet(securityAltID)) return true;
		if (FixUtils.isSet(securityAltIDSource)) return true;
		return false;
	}
	@Override
	public void encode( ByteBuffer out )
	{
		if (FixUtils.isSet(securityAltID)) FixUtils.putFixTag( out, FixTags.SECURITYALTID_INT, securityAltID, 0, Utils.lastIndexTrim(securityAltID, (byte)0) );
		if (FixUtils.isSet(securityAltIDSource)) FixUtils.putFixTag( out, FixTags.SECURITYALTIDSOURCE_INT, securityAltIDSource, 0, Utils.lastIndexTrim(securityAltIDSource, (byte)0) );
	}
	/**
	 * If you use toString for any other purpose than administrative printout.
	 * You will end up in nifelheim!
	**/
	@Override
	public String toString() {
		char sep = '\n';
		if (Boolean.getBoolean("fix.useOneLiner")) sep = ( byte )0x01;

		String s = "";

			if (FixUtils.isSet(securityAltID)) s += "SecurityAltID(455)=" + new String(securityAltID) + sep;
			if (FixUtils.isSet(securityAltIDSource)) s += "SecurityAltIDSource(456)=" + new String(securityAltIDSource) + sep;
		return s;

	}

	@Override
	public boolean equals(Object o) {
		if (! ( o instanceof SecAltIDGrp)) return false;

			SecAltIDGrp msg = (SecAltIDGrp) o;

		if (!Utils.equals( securityAltID, msg.securityAltID)) return false;

		if (!Utils.equals( securityAltIDSource, msg.securityAltIDSource)) return false;

		return true;
	}
}
}
